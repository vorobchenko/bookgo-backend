# Minimal config for Supabase GitHub integration (migrations only).
# Keep this file compatible with the hosted deploy runner — avoid bleeding-edge CLI-only keys.

project_id = "bookgo-backend"

[db]
major_version = 17

[db.migrations]
enabled = true
